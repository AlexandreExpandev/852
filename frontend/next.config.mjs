/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If your backend and frontend are on different domains in production,
  // you might need to configure rewrites or CORS settings here.
};

export default nextConfig;

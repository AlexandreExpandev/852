/**
 * @module auth
 * @summary Manages user authentication, authorization, and session management.
 * @domain security
 */

// This domain is a placeholder for future authentication features.
// It would include services for login/logout, hooks like useAuth,
// and components for login forms.

export const moduleMetadata = {
  name: 'auth',
  domain: 'security',
  version: '1.0.0',
} as const;
